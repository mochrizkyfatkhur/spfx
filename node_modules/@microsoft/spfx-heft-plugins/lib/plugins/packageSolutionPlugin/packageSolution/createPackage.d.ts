/**
 * @file createPackage.ts
 * @Copyright (c) Microsoft Corporation.  All rights reserved.
 *
 * Creates a client-side app package and writes it to disk
 */
import { IClientSideComponentManifest } from '@microsoft/sp-module-interfaces';
import { Terminal } from '@rushstack/node-core-library';
import { IPackageSolutionConfig } from '../SolutionPackager';
/**
 * Creates and writes a package to disk using a package definition and manifest mapping
 */
export default function createPackageAsync(terminal: Terminal, config: IPackageSolutionConfig, manifests: Map<string, IClientSideComponentManifest>): Promise<void>;
//# sourceMappingURL=createPackage.d.ts.map